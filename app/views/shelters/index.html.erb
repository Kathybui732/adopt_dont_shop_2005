<h1 class = "page-header">All Shelters</h1>

<ul id = "all-index">
  <% @shelters.each do |shelter| %>
  <p><%= link_to "#{shelter.name}", "/shelters/#{shelter.id}" %></p><%= link_to "Edit #{shelter.name}", "/shelters/#{shelter.id}/edit" %>
  <% end %>
</ul><br>
<br>
<%= link_to "New Shelter", '/shelters/new', method: :get %>
